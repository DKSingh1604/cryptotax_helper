rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow authenticated users to perform all operations on all collections
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}